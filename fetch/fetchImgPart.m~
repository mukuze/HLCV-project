function imgParts = fetchImgPart(imgId)

partLocs = textread('../parts/part_locs.txt');

for i=1:size(partLocs,1)
    if(imgId == partLocs(i,1))
        imgParts = partLocs(i:i+14,2:4);
        break;
    end
end

end